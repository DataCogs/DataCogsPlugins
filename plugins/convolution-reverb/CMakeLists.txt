# Part of the DataCogsPlugins suite. JUCE, Catch2 and the AAX SDK path are
# provided once by the top-level CMakeLists (CPM); this file only pulls in
# the plugin and its tests.

add_subdirectory(plugin)
add_subdirectory(test)
